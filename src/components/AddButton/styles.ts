import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    width: 125,
    height: 45,
    borderRadius: 6,
    backgroundColor: "#816AC3",
    flexDirection: 'row',
    alignItems: "center",
    padding: 10
  },

  text: {
    color: '#fff',
    marginLeft: 15,
    fontSize: 14,
    fontWeight: 'bold'
  }
})
